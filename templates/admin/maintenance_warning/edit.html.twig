{% extends 'admin/base-with-nav.html.twig' %}

{%- set translation_prefix = "admin.maintenance-warning.#{maintenanceWarning.id ? 'edit' : 'add'}" -%}

{%- block adminContent -%}
    {{ form(form) }}
{%- endblock -%}