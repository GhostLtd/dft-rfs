{% extends 'admin/base-with-nav.html.twig' %}

{%- set headingDomain = translation_domain %}
{%- set headingParams = translation_parameters %}

{%- block adminContent -%}
    {% from '@GhostGovUkFrontend/components/macros.html.twig' import warningText -%}
    {%- with {showActions: false, survey: subject, response: subject.response, marginBottom: 4} -%}
        {%- include "admin/international/includes/survey-basics.html.twig" -%}
    {%- endwith -%}

    {{ form(form) }}
{%- endblock -%}