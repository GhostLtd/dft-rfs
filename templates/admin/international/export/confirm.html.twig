{% extends 'admin/base-with-nav.html.twig' %}

{% set translation_prefix = "admin.international.export-confirm" %}
{% set headingParams = {
    year: year,
    quarter: quarter
} %}


{%- block adminContent -%}
    {% from '@GhostGovUkFrontend/components/macros.html.twig' import warningText -%}

    {{- warningText({text: "#{translation_prefix}.warning" | trans }) -}}

    {{ form(form) }}
{%- endblock -%}