{% extends 'admin/base-with-nav.html.twig' %}

{%- set translation_prefix = "admin.international.edit-trip" -%}
{%- set headingParams = {
    date: trip.outboundDate,
    origin: trip.origin,
    destination: trip.destination
} %}

{%- block adminContent -%}
    {%- form_theme form "admin/international/trip/theme.html.twig" -%}
    {{ form(form) }}
{%- endblock -%}