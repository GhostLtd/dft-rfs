{% extends 'admin/base-with-nav.html.twig' %}

{%- set translation_prefix = "admin.international.delete-vehicle" -%}
{%- set headingParams = {
    registrationMark: vehicle.registrationMark|formatRegMark
} -%}

{%- block adminContent -%}
    {% from '@GhostGovUkFrontend/components/macros.html.twig' import warningText -%}
    {%- with {showActions: false, vehicle: vehicle, marginBottom: 4} -%}
        {%- include "international_survey/includes/vehicle.html.twig" -%}
    {%- endwith -%}

    {{ warningText({text: 'This will permanently delete this vehicle and all related trips and consignment actions.'}) }}

    {{ form(form) }}
{%- endblock -%}