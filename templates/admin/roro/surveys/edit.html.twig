{%- extends 'admin/base-with-nav.html.twig' -%}

{%- set translation_prefix = "admin.roro.edit" -%}

{% form_theme form 'roro/survey/form-theme-vertical-first-collection.html.twig' %}

{%- block adminContent -%}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds-from-wide">
            {{ form(form) }}
        </div>
    </div>
{%- endblock -%}