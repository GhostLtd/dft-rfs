## GCP AppEngine config
runtime: php74
entrypoint: serve public/index.php
service: default

handlers:
  - url: /build
    static_dir: public/build
  - url: /.*
    script: auto

inbound_services:
  - warmup
